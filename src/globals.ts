const w = 400
const h = 400

  const personColor = '#8BBF9F'
  const personSize = 6
  const	wallColor = "#857E7B"
  const exitWidth = 10
  const isNode = process.title.indexOf("node") !== -1

export {
	w,
	h,
  personColor,
  personSize,
  wallColor,
  exitWidth,
  isNode,
}